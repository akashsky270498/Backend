-> Git never tracks folder it only tracks file.
-> .gitkeep file this file is used to just keep it inside any folder which is empty so that the folder gets tracked by GIT.
-> use git ignore generator.
-> npm i -D nodemon || npm i --save-dev nodemon ->> dev dependencies = used ony in dev branch
-> Use try & catch || promise for db connectivity && also use async await bcoz db is in another continent.